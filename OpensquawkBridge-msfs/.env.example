# Copy this file to ".env" and adjust values to configure the OpenSquawk Bridge.
# Lines beginning with # are comments.

# Base URL for the OpenSquawk bridge backend (defaults to https://opensquawk.de)
# BRIDGE_BASE_URL=https://opensquawk.de

# Override the telemetry endpoint URL used for sending simulator data.
# BRIDGE_TELEMETRY_URL=https://opensquawk.de/api/msfs/telemetry
# SERVER_URL=your-custom-telemetry-endpoint

# Optional bearer token for authenticated API calls.
# AUTH_TOKEN=

# Interval configuration (in seconds) for active and idle heartbeats.
# ACTIVE_INTERVAL_SEC=30
# IDLE_INTERVAL_SEC=120

# Allow the GUI to run even if the SimConnect native DLL cannot be loaded.
# Set to true/1/on when you want to ignore "Bad Image" errors.
BRIDGE_IGNORE_SIMCONNECT_LOAD_ERRORS=false
